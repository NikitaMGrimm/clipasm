[package]
name = "clipasm"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "A strict, typed, stack-based programmatic video compiler"
license = "MIT"

[lib]
name = "clipasm"
path = "src/lib.rs"

[[bin]]
name = "clipasm"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
yaml-rust2 = "0.11"

[dev-dependencies]
tempfile = "3"

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"

[lints.clippy]
all = "warn"
pedantic = "warn"
