- program:
    version: 1
    project:
      video: {width: 320, height: 180, fps: 24}
    inputs:
      - video: Video
    parameters:
      range:
        type: TimeRange
      count:
        type: Integer

- trim:
    video: $video
    range: $range
- repeat: $count
